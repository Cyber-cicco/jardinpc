{
    "database" : {
        "db-user" : "${DB_USER_JARDIN}",
        "db-password" : "${DB_PASS_JARDIN}",
        "db-name" : "jardinpc",
        "db-adress" : "${DB_HOST}"
    },
    "jwt" : {
        "secret" : "${JARDIN_JWT_SECRET}"
    }
}

